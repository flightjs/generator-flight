{
  "name": "<%= appname %>",
  "version": "0.0.0",
  "devDependencies": {
    "generator-flight": "~<%= genVersion %>",
    "karma": "~0.8.5"
  },
  "scripts": {
    "test": "./node_modules/.bin/karma start --single-run --browsers PhantomJS"
  }
}
